import Link from "next/link"; import {Footer,Header,Disclosure,Newsletter} from "@/components"; import {publicProducts} from "@/lib/public-products";
export const dynamic="force-dynamic"; export default function Home(){const p=publicProducts();const picks=[p.find(x=>x.slug==="soundcore-p30i"),p.find(x=>x.slug==="qcy-melobuds-pro"),p.find(x=>x.slug==="sony-wh-1000xm5")].filter(Boolean);return <><Header/><main id="conteudo"><section className="hero"><div className="shell"><p className="eyebrow">Guia de compra</p><h1>Encontre o fone certo sem perder tempo</h1><p className="lead">Comparamos bateria, conforto, cancelamento de ruído e custo-benefício para ajudar você a escolher.</p><Link className="cta" href="/melhores-fones-mercado-livre">Ver os melhores fones</Link></div></section><section><div className="shell"><h2>Três escolhas para começar</h2><div className="grid">{picks.map(x=>x&&<Link className="card featured-card" key={x.id} href={`/fones/${x.slug}`}><div className="product-thumb">{x.image?<img src={x.image} alt={x.alt}/>:<span>{x.name}</span>}</div><p className="category">{x.badge}</p><h3>{x.name}</h3><p>{x.description}</p></Link>)}</div></div></section><section><div className="shell"><h2>Escolha pelo seu uso</h2><div className="grid compact-grid">{p.slice(0,6).map(x=><Link className="card" key={x.id} href={`/melhores-fones-mercado-livre#${x.slug}`}><p className="category">{x.category}</p><h3>{x.name}</h3></Link>)}</div></div></section><section><div className="shell"><h2>Como avaliamos</h2><p className="lead">Priorizamos conforto, bateria, recursos que ajudam no dia a dia e a relação entre o que cada modelo entrega e seu segmento.</p><Disclosure/></div></section><div className="shell"><Newsletter/></div></main><Footer/></>}
