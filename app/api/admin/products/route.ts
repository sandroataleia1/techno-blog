import {NextResponse} from "next/server"; import {db,listProducts} from "@/lib/db"; import {id,requireAdmin,validAffiliate} from "@/lib/admin";
function text(v:unknown,max=4000){return typeof v==="string"&&v.trim()&&v.length<=max?v.trim():null} function array(v:unknown){return Array.isArray(v)&&v.every(x=>typeof x==="string"&&x.length<=300)?v:null} function specs(v:unknown){return v&&typeof v==="object"&&!Array.isArray(v)&&Object.entries(v).every(([k,x])=>k.length<100&&typeof x==="string"&&x.length<500)?v:null}
function validate(b:Record<string,unknown>){const name=text(b.name,160),slug=text(b.slug,120),brand=text(b.brand,100),category=text(b.category,160),badge=text(b.badge,160),short=text(b.shortDescription),full=text(b.fullDescription,12000),alt=text(b.imageAlt,300);const position=Number(b.position),benefits=array(b.benefits),attention=array(b.attentionPoints),recommended=array(b.recommendedFor),notRecommended=array(b.notRecommendedFor),specifications=specs(b.specifications);if(!name||!slug||!/^[a-z0-9]+(?:-[a-z0-9]+)*$/.test(slug)||!brand||!category||!badge||!short||!full||!alt||!Number.isInteger(position)||position<1||!benefits||!attention||!recommended||!notRecommended||!specifications||!validAffiliate(String(b.affiliateUrl))||!validAffiliate(String(b.searchUrl)))throw new Error("Dados inválidos. Confirme campos obrigatórios, slug e URLs HTTPS do Mercado Livre.");return {name,slug,brand,category,badge,short,full,alt,position,benefits,attention,recommended,notRecommended,specifications}}
export async function GET(){try{await requireAdmin();return NextResponse.json(listProducts())}catch{return NextResponse.json({error:"Não autorizado"},{status:401})}}
export async function POST(req:Request){try{await requireAdmin();const raw=await req.json(),p=validate(raw),now=new Date().toISOString();const record={id:id(),name:p.name,slug:p.slug,brand:p.brand,position:p.position,category:p.category,badge:p.badge,short_description:p.short,full_description:p.full,benefits:JSON.stringify(p.benefits),attention_points:JSON.stringify(p.attention),specifications:JSON.stringify(p.specifications),recommended_for:JSON.stringify(p.recommended),not_recommended_for:JSON.stringify(p.notRecommended),affiliate_url:raw.affiliateUrl,search_url:raw.searchUrl,image_alt:p.alt,is_active:raw.isActive?1:0,is_featured:raw.isFeatured?1:0,created_at:now,updated_at:now};db().prepare(`INSERT INTO products (${Object.keys(record).join(",")}) VALUES (${Object.keys(record).map(x=>`@${x}`).join(",")})`).run(record);return NextResponse.json(record,{status:201})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Não foi possível salvar"},{status:e instanceof Error&&e.message==="UNAUTHORIZED"?401:400})}}
