import {NextResponse} from "next/server"; import {requireAdmin} from "@/lib/admin"; import {restoreBackup} from "@/lib/backups"; export async function POST(req:Request){try{await requireAdmin();const body=await req.json();if(body.confirm!=="RESTAURAR"||typeof body.name!=="string")throw new Error("Confirmação inválida.");return NextResponse.json({ok:true,safety:restoreBackup(body.name)})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Restauração recusada"},{status:400})}}
