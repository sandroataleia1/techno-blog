import {NextResponse} from "next/server"; import {authStatus,requireAdmin,requireAdminMutation} from "@/lib/admin"; import {createBackup,listBackups} from "@/lib/backups"; export async function GET(){try{await requireAdmin();return NextResponse.json(listBackups())}catch{return NextResponse.json({error:"Não autorizado"},{status:401})}} export async function POST(req:Request){try{await requireAdminMutation(req);return NextResponse.json(createBackup(),{status:201})}catch(e){return NextResponse.json({error:"Não foi possível gerar backup"},{status:authStatus(e)})}}
