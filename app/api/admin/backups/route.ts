import {NextResponse} from "next/server"; import {requireAdmin} from "@/lib/admin"; import {createBackup,listBackups} from "@/lib/backups"; export async function GET(){try{await requireAdmin();return NextResponse.json(listBackups())}catch{return NextResponse.json({error:"Não autorizado"},{status:401})}} export async function POST(){try{await requireAdmin();return NextResponse.json(createBackup(),{status:201})}catch{return NextResponse.json({error:"Não foi possível gerar backup"},{status:400})}}
