"use client"; import {useState} from "react"; import type {FormEvent} from "react"; import {useRouter} from "next/navigation";
export default function Login(){const [error,setError]=useState(""),router=useRouter();async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),r=await fetch("/api/admin/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:f.get("email"),password:f.get("password")})});if(r.ok)router.push("/admin");else setError((await r.json()).error||"Não foi possível entrar.")}return <main className="article admin-login"><div className="shell" style={{maxWidth:520}}><h1>Administração</h1><form className="card" onSubmit={submit}><label>E-mail<input name="email" type="email" required /></label><label>Senha<input name="password" type="password" required /></label><button className="cta">Entrar</button>{error&&<p role="alert">{error}</p>}</form></div></main>}
