import Database from "better-sqlite3"; import {mkdirSync} from "node:fs"; import {dirname,resolve} from "node:path"; const file=process.env.DATABASE_PATH||resolve("data/guia-do-fone.sqlite");mkdirSync(dirname(file),{recursive:true});const db=new Database(file);db.exec("CREATE TABLE IF NOT EXISTS schema_migrations (id TEXT PRIMARY KEY, applied_at TEXT NOT NULL)");console.log(`Migrations initialized at ${file}`);db.close();
